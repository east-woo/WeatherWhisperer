// Toolbar.jsx
import React from 'react';

const Toolbar = () => {
    return (
        <div className="toolbar">
            {/* Your toolbar content goes here */}
        </div>
    );
};

export default Toolbar;